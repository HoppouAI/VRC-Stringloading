# hotfix list for the custom tag filter, loaded on top of the list baked into the world
# the big list lives in the world itself, this is for adding stuff between world uploads
# one entry per line, lowercase, letters and digits only (spaces, dots and symbols get folded out anyway)
# plain line = blocked anywhere inside the text
# =word = blocked only as a whole word (use for short stuff like =ass that sits inside normal words)
# +word = allowed word, pulled out before checking (scunthorpe style false positives)
# ~text = blocked in the raw lowercased text with spaces removed, for links and domains
allahu
